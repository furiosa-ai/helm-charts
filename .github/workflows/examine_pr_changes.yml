name: Helm

on:
  push:
    paths: ["charts/**", ".github/workflows/examine_pr_changes.yml", ".github/workflows/kind_config/**"]
    branches: [ "main" ]
  pull_request:
    paths: ["charts/**", ".github/workflows/examine_pr_changes.yml", ".github/workflows/kind_config/**"]
    branches: [ "main" ]

jobs:
  chart-lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: setup helm env
        uses: helm/chart-testing-action@v2.8.0
      - name: Add dependency chart repos
        run: |
          helm repo add nfd https://kubernetes-sigs.github.io/node-feature-discovery/charts
          helm repo add bitnami https://charts.bitnami.com/bitnami
      - name: helm lint
        run: ct lint --validate-maintainers=false --target-branch main

  chart-test-with-kind:
    needs: chart-lint
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        k8s-version:
#          - v1.24.17
#          - v1.25.16
#          - v1.26.15
#          - v1.27.16
#          - v1.28.15
#          - v1.29.14
#          - v1.30.13
#          - v1.31.14
#          - v1.32.11
#          - v1.33.12
          - v1.34.11
          - v1.35.8
          - v1.36.4
          - v1.37.0
    steps:
      - name: checkout
        uses: actions/checkout@v6
      - name: create kind cluster
        uses: helm/kind-action@v1.15.0
        with:
          cluster_name: helm-charts-test-${{ matrix.k8s-version }}
          config: .github/workflows/kind_config/kind_${{ matrix.k8s-version }}.yaml
          kubectl_version: ${{ matrix.k8s-version }}
      - name: setup helm env
        uses: helm/chart-testing-action@v2.8.0
      - name: Add dependency chart repos
        run: |
          helm repo add nfd https://kubernetes-sigs.github.io/node-feature-discovery/charts
          helm repo add bitnami https://charts.bitnami.com/bitnami
      - name: Wait for nodes to be ready
        run: |
          kubectl wait --for=condition=Ready nodes --all --timeout=300s
      - name: try install
        run: ct install --chart-dirs charts --charts charts/furiosa-device-plugin,charts/furiosa-feature-discovery,charts/furiosa-metrics-exporter,charts/furiosa-npu-operator --namespace kube-system  --helm-extra-args '--timeout 500s'
